# @package _global_

defaults:
  - _self_
  - data: rsna2024 # choose datamodule with `test_dataloader()` for evaluation
  - model: timm_model
  - logger: wandb
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  - experiment: null
  - callbacks: default

task_name: "eval"

tags: ["dev"]

# seed for random number generators in pytorch, numpy and python.random
seed: 0

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
