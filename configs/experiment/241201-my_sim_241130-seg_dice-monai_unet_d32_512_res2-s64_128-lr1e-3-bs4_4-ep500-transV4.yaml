# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /callbacks: default_save20ep
  - override /data: czii_my_sim
  - override /model: monai_unet_seg
  - override /model/criterion_seg: dice

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["baseline"]

callbacks:
  early_stopping:
    patience: ${trainer.max_epochs}

model:
  is_kaggle_data: false
  num_classes: ${data.num_classes}
  eval_normalize: true
  scheduler:
    num_warmup_steps: 10
  optimizer:
    lr: 1e-3
  # optimize_config:
  #   mode: target_decay
  #   target_name: net.encoder
  #   lr_base: ${model.optimizer.lr}
  #   lr_decay_coef: 0.1
  net:
    num_classes: ${data.num_classes}
    channels: [32, 64, 128, 256, 512]
    output_classmap: false
    output_seg: true
    output_det: false
    num_res_units: 2

data:
  volume_size: [64, 128, 128]
  batch_size: 4
  num_crop_samples: 1  # 増やすと1コアでの処理時間が増える
  transforms_version: 4

trainer:
  max_epochs: 500
  accumulate_grad_batches: 4
  check_val_every_n_epoch: 10
  gradient_clip_val: 3.0 

experiment_name: 241201-my_sim_241130-seg_dice-monai_unet_d32_512_res2-s64_128-lr1e-3-bs4_4-ep500-transV4
logger:
  wandb:
    tags: ${tags}
    name: ${experiment_name}-fold${data.fold}
log_dir: ${paths.log_dir}/${task_name}/runs/${experiment_name}
hydra:
  run:
    dir: ${log_dir}
