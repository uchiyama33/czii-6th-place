# pip download -d packages/ -r requirements.txt
# pip install --no-index --find-links=./packages -r requirements.txt 
# /root/.cache/pip/wheels にあるビルド済みwhlを./packagesにコピーする

--extra-index-url https://download.pytorch.org/whl/cu121

# --------- pytorch --------- #
torch==2.5.1
torchvision==0.20.1
lightning>=2.0.0
torchmetrics>=0.11.4

# --------- hydra --------- #
hydra-core==1.3.2
hydra-colorlog==1.2.0
hydra-optuna-sweeper==1.2.0

# --------- loggers --------- #
# wandb
# neptune-client
# mlflow
# comet-ml
# aim>=3.16.2  # no lower than 3.16.2, see https://github.com/aimhubio/aim/issues/2550

# --------- others --------- #
rootutils       # standardizing the project root setup
pre-commit      # hooks for applying linters on commit
rich            # beautiful text formatting in terminal
pytest          # tests
# sh            # for running bash commands in some tests (linux/macos only)

monai 
zarr 
numpy<2
cryoet-data-portal==4.0.0 
s3fs 
trimesh 
scikit-image 
pyside2
numcodecs 
connected-components-3d
copick 
packaging==23.2

tensorrt-cu12==10.3.0
torch-tensorrt==2.5.0
einops
timm-3d

# tensorrt_cu12_libsの対処
# # 1. build ツールのインストール
# pip install build

# # 2. tar.gz の解凍
# tar -xzf /workspace/pip_packages/packages/tensorrt_cu12_libs-10.3.0.tar.gz -C /workspace/pip_packages/packages/

# # 3. ディレクトリ移動と Wheel のビルド
# cd /workspace/pip_packages/packages/tensorrt_cu12_libs-10.3.0
# python -m build --wheel

# # 4. .whl ファイルの移動
# mv dist/*.whl /workspace/pip_packages/packages/